export enum Gender {
    MALE = "Male",
    FEMALE = "Female",
    OTHER = "Other"
}

export enum Role {
    ADMIN = "admin",
    USER = "user"
}

export enum OrderStatus {
    PENDING = "pending",
    PROCESSING = "processing",
    SHIPPED = "shipped",
    DELIVERED = "delivered",
    CANCELLED = "cancelled"
}

export enum PaymentStatus {
    PENDING = "pending",
    SUCCESS = "success",
    FAILED = "failed"
}

export enum PaymentMethod {
    COD = "cod",
    MOMO = "momo"
}


